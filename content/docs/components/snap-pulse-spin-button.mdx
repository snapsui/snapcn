---
title: Pulse Spin Button
date: 2024-10-20
description: The Pulse Spin Button combines an animated spin and pulse effect for an engaging interaction.
author: jeet1desai
published: true
---

<ComponentPreview name="snap-pulse-spin-button-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx snapcn add snap-pulse-spin-button
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install dependency.</Step>

```bash
npm install framer-motion clsx tailwind-merge
```

<Step>Add util file.</Step>

```typescript
import clsx, { ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="snap-pulse-spin-button" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>
