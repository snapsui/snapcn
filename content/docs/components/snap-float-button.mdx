---
title: Float Button
date: 2024-09-25
description: The Float Button provides quick access to actions with a dynamic expanding design.
author: jeet1desai
published: true
---

<ComponentPreview name="snap-float-button-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx snapcn add snap-float-button
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install dependency.</Step>

```bash
npm install framer-motion clsx tailwind-merge
```

<Step>Add util file.</Step>

```typescript
import clsx, { ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="snap-float-button" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

### Usage

```javascript
import {
  SnapFloatButton,
  SnapFloatButtonItem,
} from "@/components/snapui/snap-float-button";

<SnapFloatButton>
  <SnapFloatButtonItem Icon={GitHubLogoIcon} onClick={() => {}} />
  <SnapFloatButtonItem Icon={DiscordLogoIcon} onClick={() => {}} />
  <SnapFloatButtonItem Icon={NotionLogoIcon} onClick={() => {}} />
  <SnapFloatButtonItem Icon={OpacityIcon} onClick={() => {}} />
</SnapFloatButton>;
```
